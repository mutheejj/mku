import java.swing.*;
import java.awt.*;
import java.awt.event.*;

public class JOptionPlaneDemo
{
    static JTextField uname;
    public static void main(String[] args)
    {
        JFrame jf = new JFrame();
        jf.setsize(250, 250);
        Flowlayout = obj = new Flowlayout();
        jf.setlayyout(ob);
        Jlable lb.name = new JButton("submit");
        myListerner obj = new myListerner();
        submit.addActionLister(obj);
        jf.add(lbname);
        jf.add(submit);
        jf.setvisible(true);
    }
}
